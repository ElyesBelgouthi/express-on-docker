apiVersion: v1
kind: Service
metadata:
  name: {{ include "express-demo-app.fullname" . }}
  labels:
    {{- include "express-demo-app.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "express-demo-app.selectorLabels" . | nindent 4 }}
